import Head from '@/components/head';
import SectionSchedule from '@/pages/schedule/components/section-schedule';

export default function Schedule() {
  return (
    <main>
      <Head title="Schedule" description="Schedule" />
      <SectionSchedule />
    </main>
  );
}
